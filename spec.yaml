name: zero2prod
region: lon
services:
  - name: zero2prod
    dockerfile: Dockerfile
    github:
      branch: main
      deploy_on_push: true 
      repo: BeniiAlCo/zero2prod
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
